query notifications {
  notifications {
    edges {
      node {
        id
        ...CommentNotification
        ...PostNotification
        ...RequestNotification
        ...SenderNotification,
        ... RecipientNotification
        type
        createdAt
      }
    }
  }
}